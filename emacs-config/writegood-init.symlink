;;; -*- mode: emacs-lisp; -*-

(add-hook 'writegood-mode-hook (lambda()
	  (local-set-key (kbd "C-c C-g g") 'writegood-grade-level)
	  (local-set-key (kbd "C-c C-g e") 'writegood-reading-ease)))

(provide 'writegood-init)
;;; writegood-init.symlink ends here
